---
import ToolIcons from './ToolIcons.astro';

interface Props {
	context: string;
	scope: string;
	duration: string;
	role: string;
	tools: Array<'figma' | 'github'>;
}

const { context, scope, duration, role, tools } = Astro.props;
---

<section class="project-meta">
	<p class="project-meta__context">{context}</p>

	<dl class="project-meta__facts">
		<div class="project-meta__row">
			<dt>Scope:</dt>
			<dd>{scope}</dd>
		</div>
		<div class="project-meta__row">
			<dt>Duration:</dt>
			<dd>{duration}</dd>
		</div>
		<div class="project-meta__row">
			<dt>Role:</dt>
			<dd>{role}</dd>
		</div>
		<div class="project-meta__row project-meta__row--tools">
			<dt>Tools:</dt>
			<dd><ToolIcons tools={tools} /></dd>
		</div>
	</dl>
</section>

<style>
	.project-meta {
		display: grid;
		grid-template-columns: 2fr 1fr;
		gap: 2rem;
		padding: 1.5rem;
		margin-bottom: 3rem;
	}

	.project-meta__context {
		margin: 0;
		font-size: 1rem;
	}

	.project-meta__facts {
		margin: 0;
		padding-left: 2rem;
		border-left: 1px solid var(--color-text);
		font-size: 1rem;
	}

	.project-meta__row {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		margin-bottom: 1.5rem;
	}

	.project-meta__row:last-child {
		margin-bottom: 0;
	}

	.project-meta__row--tools {
		flex-direction: column;
		align-items: flex-start;
		gap: 0.75rem;
	}

	.project-meta__row dt {
		font-weight: 600;
	}

	.project-meta__row dd {
		margin: 0;
	}
</style>
